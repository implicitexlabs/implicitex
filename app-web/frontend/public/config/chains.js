// Active chain config — transfersEnabled: false until testnet gates are complete.
// Populate supportedChains and set transfersEnabled: true only after testnet signoff.
// See chains.example.js for the expected shape.
// This file is gitignored after real addresses are added; chains.example.js documents the shape.
const ImplicitExChains = {
  transfersEnabled: false,
  supportedChains: {}
};
